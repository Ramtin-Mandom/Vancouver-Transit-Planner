DB_HOST=localhost
DB_PORT=5432
DB_NAME=vancouver_transit
DB_USER=transit_app
DB_PASSWORD=replace_with_a_local_password
# Optimistic A* speed bound in km/h.
ROUTING_MAX_TRANSIT_SPEED_KMH=120
TRANSLINK_API_KEY=
# Optional; must retain the {api_key} placeholder.
TRANSLINK_GTFS_RT_URL=https://gtfsapi.translink.ca/v3/gtfsrealtime?apikey={api_key}
# Optional reliability policy.
RELIABILITY_MINIMUM_SAMPLES=10
RELIABILITY_EARLY_SECONDS=-120
RELIABILITY_LATE_SECONDS=300
RELIABILITY_SHRINKAGE_STRENGTH=20

# Process-local routing caches (safe defaults for a small Render instance)
# Per API request, cache_mode=request bypasses these and reproduces the old path.
ROUTING_TRIP_CACHE_CAPACITY=750
ROUTING_TRIP_CACHE_TTL_SECONDS=3600
ROUTING_NEGATIVE_TRIP_CACHE_TTL_SECONDS=60
ROUTING_DEPARTURE_CACHE_CAPACITY=300
ROUTING_SERVICE_DAY_CAPACITY=1
ROUTING_HEURISTIC_CACHE_CAPACITY=7500
ROUTING_PROFILE_CACHE_CAPACITY=5000
ROUTING_RESPONSE_CACHE_ENABLED=true
ROUTING_RESPONSE_CACHE_CAPACITY=75
ROUTING_RESPONSE_CACHE_TTL_SECONDS=60

# Startup cache warming. Only the small static stop/transfer snapshot is warm;
# per-stop departures and trip chains remain frontier-driven.
CACHE_WARMUP_ENABLED=true
CACHE_WARMUP_BLOCK_READINESS=false
CACHE_WARMUP_TODAY_INDEX=false
CACHE_WARMUP_SKYTRAIN=false
CACHE_WARMUP_SKYTRAIN_MAX_TRIPS=100
CACHE_WARMUP_SKYTRAIN_START_MINUTES=0
CACHE_WARMUP_SKYTRAIN_HORIZON_MINUTES=120
CACHE_WARMUP_SKYTRAIN_MEMORY_BUDGET_MB=24
CACHE_WARMUP_TOMORROW_INDEX=false
CACHE_WARMUP_TIMEOUT_SECONDS=30
